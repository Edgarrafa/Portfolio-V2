import Image from "next/image";
import { cn } from "@/lib/utils";

export default function Home() {
  return (
    <div className={cn("flex flex-col flex-1 items-center justify-center bg-zinc-50 font-sans dark:bg-black")}>
      <main className={cn("flex flex-1 w-full max-w-3xl flex-col items-center justify-between py-32 px-16 bg-white dark:bg-black sm:items-start")}>
        
      </main>
    </div>
  );
}
